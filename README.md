# sx-request-delayed-example

This repo contains a notebook with an example spec using a simple funcADL query but on 360 samples.
Sending it to the backend with `deliver(spec)` yields no reaction from the server for at least 10 minutes. At this point, the transformations start immediately and are available on the dashboard

- To start: `pixi run lab`
